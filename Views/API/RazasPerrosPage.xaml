<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             x:Class="ProyectoP2.RazasPerrosPage"
             Title="Adoptar">
    <StackLayout>
        <Label Text="Si lo has visto, llama al siguiente número +593 98 989 8290" 
               FontSize="Subtitle" HorizontalOptions="CenterAndExpand" Margin="0,20,0,0" />
        <ListView x:Name="RazasListView">
            <ListView.ItemTemplate>
                <DataTemplate>
                    <TextCell Text="{Binding}" />
                </DataTemplate>
            </ListView.ItemTemplate>
        </ListView>
        <Button Text="Siguiente" Clicked="OnMostrarSiguienteImagenClicked" />
        <Image x:Name="ImagenPerro" Aspect="AspectFit" />
    </StackLayout>
</ContentPage>