<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             x:Class="ProyectoP2.RazasPerrosPage"
             Title="Adoptar"
             BackgroundColor="Wheat">
    <StackLayout>
        <Label Text="Si lo has visto, llama al siguiente número +593 98 989 8290" 
               FontSize="Subtitle" HorizontalOptions="CenterAndExpand" Margin="0,20,0,0" TextColor="Black" />
        <ListView x:Name="RazasListView">
            <ListView.ItemTemplate>
                <DataTemplate>
                    <TextCell Text="{Binding}" />
                </DataTemplate>
            </ListView.ItemTemplate>
        </ListView>
        <Image x:Name="ImagenPerro" Aspect="AspectFit" HeightRequest="350" WidthRequest="350" />
        <Button Text="Siguiente" Clicked="OnMostrarSiguienteImagenClicked" BackgroundColor="#8B4513" TextColor="White" />



    </StackLayout>
</ContentPage>